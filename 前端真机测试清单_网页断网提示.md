# 前端真机测试清单 · 网页端断网错误提示

> 给安卓 App（前端 CC）在**真机**上跑的验收清单。对应改动：`consultant.html` 里所有联网请求（fetch）
> 在**断网失败**时弹统一 toast **"⚠ 网络断开，请检查网络后重试"**，不再静默无反应。**后端无改动**。
> 已上线（commit `41d3e84`，online-0527）。配套安卓端"断网保界面/录音不停/恢复自动补传"。

---

## 0. 改动要点（前端理解用）

- **一处拦截、全覆盖**：包了一层 `window.fetch`，**所有联网操作**（28 处 fetch + 以后新增）断网失败都会弹同一个 toast，再把错误原样抛出 → 既有逻辑不变。
- **只在真断网时弹**：fetch 只有"网络层失败（断网/DNS/连接被拒）"才 reject；服务器返回 4xx/5xx **不会**触发这个 toast（那不是断网）。
- **不刷屏**：单一 toast 元素 + 重置计时，断网期间（含 5s 自动刷新反复失败）只常驻一条，不堆叠；恢复联网后最后一次失败 **3.5 秒**自动消失。
- **试听例外处理**：`<audio>` 试听是直接拉 OSS 链接（非 fetch），断网播放失败也会弹，但**仅在确实离线时**（避免签名链接过期 403 被误判成断网）。

**怎么制造断网**：开**飞行模式**，或关 WiFi+移动数据。恢复就关掉飞行模式。

---

## 场景 1 · 刷新未归档列表（断网）

**做什么**：在顾问页停留（未归档列表每 5s 自动刷新）→ 开飞行模式。
**应看到**：几秒内底部弹红色 toast **"⚠ 网络断开，请检查网络后重试"**；且**不刷屏**——反复刷新只让这一条常驻，不会叠出一排。
**恢复**：关飞行模式 → 列表恢复自动刷新，toast 3.5s 后消失。

---

## 场景 2 · 试听（断网）

**做什么**：断网状态下，点未归档/接诊里某条录音的**试听**播放。
**应看到**：弹 **"⚠ 网络断开，无法试听，请检查网络后重试"**（音频本来就放不出来，但现在有明确提示，不是点了没反应）。
**恢复**：联网后再点 → 正常播放。

---

## 场景 3 · 绑定顾客（断网）

**做什么**：断网下走"绑定顾客"（选/新建顾客后点确认绑定）。
**应看到**：点确认 → 弹"网络断开"toast；**没有**误以为绑成功（不会错误地关弹窗显示成功）。
**恢复**：联网后重试 → 正常绑定。

> 同类一并测：**换绑**、**退回未归档**、**确认说话人**、**新增/编辑当日接诊**、**搜索顾客/客户**——断网点都应弹同一提示。

---

## 场景 4 · 触发 / 重跑分析（断网）

**做什么**：断网下点"待分析 / ⟳ 重跑 / 开始分析"。
**应看到**：弹"网络断开"toast；不会假装已开始分析。
**恢复**：联网后重试 → 正常触发。

---

## 场景 5 · 处理提醒（断网）

**做什么**：断网下点提醒里的"已读/已处理"类按钮。
**应看到**：弹"网络断开"toast。
**恢复**：联网后正常处理。

---

## 场景 6 · 恢复联网后一切正常（回归）

**做什么**：从断网恢复联网。
**应看到**：
- toast 自动消失（最后一次失败后 3.5s）。
- 未归档列表恢复自动刷新；之前断网点失败的操作，重试都能成功。
- **联网正常状态下，所有操作不应莫名弹"网络断开"**（不误报）。

---

## 场景 7 · 不误报（重要的负向测试）

**目的**：确认这个 toast **只**在真断网时出现，不抢其它正常报错。

**做什么 & 应看到**（都在**联网正常**下做）：
1. 触发一个**服务器会拒绝**的操作（如绑定时漏填必填项、重复操作等）→ 应是该操作**原有的报错/提示**，**不应**弹"网络断开"。
2. 正常浏览、刷新、试听、绑定 → 全程**不弹**"网络断开"。

> 原理：服务器返回 4xx/5xx 时 fetch 不会 reject，不触发断网 toast；只有连不上服务器才弹。

---

## 8. 自检小抄（跑完勾一遍）

- [ ] 断网时未归档自动刷新 → 弹"网络断开"，且不刷屏（只一条常驻）
- [ ] 断网点试听 → 弹"无法试听/网络断开"
- [ ] 断网点绑定/换绑/退回/确认说话人/新增接诊/搜索 → 都弹"网络断开"
- [ ] 断网点触发/重跑分析 → 弹"网络断开"
- [ ] 断网点处理提醒 → 弹"网络断开"
- [ ] 恢复联网 → toast 自动消失、列表恢复刷新、重试都成功
- [ ] **联网正常时全程不误弹**；服务器业务报错走它自己的提示（非"网络断开"）
- [ ] 配套验证：断网时录音界面不白屏、按笔录音照常、恢复后自动补传不丢（安卓端能力）

> 任一项不对，把**操作 + 现象 + 截图**发我；若怀疑是请求本身的问题，附上**接口名 / 顾问名 / 大致时间**，我从后端日志侧帮看。
