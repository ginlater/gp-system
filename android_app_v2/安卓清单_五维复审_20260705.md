# 安卓 v2 五维复审清单（2026-07-05 晚，四批52项修完后的第二轮审核，只登记未修复）

五个审核智能体（蓝牙连接 / 录音同步上传 / 报告分析 / 绑定全流程 / 后台保活）对 8fc8673 交叉复审，
原始发现约 40 条，去重合并后 **31 项（P0×1、P1×15、P2×15）**。含 6 处四批修复的回归/不完整。
基路径与缩写同六维清单：SPC=soni/SoniPenController.java，PMS=recording/PhoneMicService.java，
RCI=recording/RecordingControllerImpl.kt，PKS=recording/PenKeepAliveService.java，BCVm=ui/bind/BindCustomerViewModel.kt。

> **真实用户案例（李雪雪 6-14，本轮导火索）**：昨晚测批次三时"绑定→退回"留下 0 录音空壳
> session 100341；列表不过滤空壳、报告页无空态还给"重新分析"（点了会写成永久 stuck）、
> 删除客户端说要审批服务端却 404——顾问端对空壳完全无出口。对应下表 G1-G3。
> 数据库存量空壳待批量清理（运维项，见文末）。

## P0 · 会丢正在录的音频

| # | 位置 | 问题 |
|---|------|------|
| Z1 | PMS:78-81 | **上传中点开始=假录音**：uploading 期间收到 ACTION_START 被拒，却广播 STATE_RECORDING——UI 进录音态、计时自走，麦克风没开，下一位顾客整段从未采集。弱网上一段传几分钟+任一笔事件把 Uploading 顶回 Idle 即触发。修：拒绝分支按真实状态广播 ERROR"上一段还在保存"；根治=上传与录音解耦 |

## A. 蓝牙连接（P1×5、P2×6）

| # | 位置 | 问题 |
|---|------|------|
| A1 | SPC:417,2325 + SoniScanActivity:86-92 | **connectTo 乐观预写 currentMac 打穿 B4**：双笔时点笔B，currentMac/prefs 秒变B，connectWatch 拿 A 的心跳+被覆写的 mac 判"成功"——仍会录进另一支+记错 mac。修：currentMac 只由真回包写；连异 mac 前先 closeConnect+清 verified |
| A2 | SPC:1459,582 | **B3 反噬（回归）**：cmd9 单次回"未在录"即收尾（iOS 要求连续2次）；固件抖动一次 → 收尾后下载"仍在增长的文件"当完整版占坑 → 真完整版被 pen_file 去重挡死，**尾段永久丢**。修：流未停时 cmd9=0 连续2次才收尾；收尾后仍有帧到达强制复核 |
| A3 | SPC:515,590,1486 | **断链清理不对称**：STATE_OFF/心跳失联不清 workerBusy/waitingForFile、文件列表首包无超时 → 重连后 kickWorker 见 busy 直接 return，补传队列永久卡死+FGS 白挂。修：两分支补 cmd2-else 同款清理；列表请求加 10s 超时 |
| A4 | RCI:351-378 + SPC:791 | **重连宽限被看门狗误杀**：断链 25s 宽限的"录音继续中"占位态 ≤3s 就被一致性看门狗拉回 Idle。修：宽限期打标豁免 |
| A5 | SPC:1221,1443 | **A3 防线漏缝**：cmd11 回填的文件名、接管会话全程都不 saveActiveSession → 进程死+笔自停仍静默漏传（正是 A3 要保的场景）。修：两处拿到文件名立即落盘 |
| A6 | SPC:1391 | 幽灵暂停：rs=2 无 sessionActive 守卫，penRecording 永久置 true → 假"录音中"UI+下载全停。修：!sessionActive 不置位 |
| A7 | SPC:1275 | 握手只发一轮初始化，缺 iOS 的"+5s 未验证补发"→通道未稳整批丢就得等超时重来。修：+5s 补发一轮 |
| A8 | SPC:415,488 | 无连接闸门：手动连接进行中 15s 重连 tick 仍插入扫描打断。修：connectTo 挂 10s 闸门+暂停重连 |
| A9 | SPC:1341,772 | 笔键新段/接管段不清 lastPenDurationSec/autoStoppedAt90 残值 → 时长虚报/新段秒停/失去90分钟保护。修：所有新段入口统一清零 |
| A10 | RCI:416 | startCompanion 用瞬时 isPenAlive 判连接，单次心跳丢包就弹扫描页死胡同（已连的笔不广播）。修：改判 linkUp |
| A11 | SPC:183,1551 | 补传任务不记归属笔 MAC：双笔换连后 3 轮找不到文件即删占位放弃。修：任务落盘带 mac，非归属笔不计 giveup |

## B. 录音同步/上传（P1×4、P2×5）

| # | 位置 | 问题 |
|---|------|------|
| B1 | Uploader+PMS:180+RCI:591 ↔ webapp:9765 | **手机麦上传不带 source=phone（D12 不完整）**：服务端按 allow_pen_rec 校验——只开手机录音权限的顾问占位建得成、正式上传恒 403，音频永远传不上。修：上传两条路径补 source 字段 |
| B2 | SPC:985,1744,1929 | **E8 回归**：占位挪独立线程后失去顺序保证——直传 300ms 后读 placeholderId 可能还是 -1 → 服务端新建行+孤儿占位；用户恰好绑了孤儿占位则**绑定行永挂"同步中"**。修：直传前合流占位 Future；pid 迟到且任务已终结时补发取消 |
| B3 | webapp:9884 + PMS:182 | 手机麦补传无幂等：上传成功但响应丢失 → 补传带同 pid 重传落新行，**同段两行可分别绑不同顾客**。修：服务端 pid 已 done 直接回 deduped |
| B4 | webapp:10186 ↔ SPC:214 | **E6 被服务端反杀（回归）**：客户端放弃烧钟 6h，服务端拉列表仍按 2h 清占位——"同步中"行 2h 照样集体消失、传成后又冒回。修：服务端 cutoff ≥6h 对齐 |
| B5 | SPC:1373 | B6 串音残留：停止帧丢+快速开新段（<6s）时新段帧已写进旧段 buffer——旧段尾混入下段开头。修：文件名变更按估算截断 buffer 或旧段一律补下载 |
| B6 | SPC:2061,736 | clearActiveSession 只在正常收尾调：放弃/断链场景不清 → 冷启动无限复活"N段同步中"badge。修：drop/成功入队时同步清 |
| B7 | Uploader:193 + SPC:1752 | 403 一律映射"登录已失效"：权限被关的顾问每 ≤2 分钟整文件白传永不止+误导重登提示。修：403 透传原话按永久失败 |
| B8 | PMS:196-225 | onRecorderError/录满90分钟只留文件不建占位不上传，提示"到待整理查看"但那里什么都没有。修：两路径也走 uploadAndFinish |
| B9 | PMS:167 | D12 两窗口：rename 失败 pid 只在内存；建占位到 rename 间被杀丢 pid → 重复行+占位泄漏。修：pid↔文件名映射落 prefs |

## C. 绑定/换绑/删除（P1×5、P2×3）

| # | 位置 | 问题 |
|---|------|------|
| C1 | webapp:10489-10560 | **bind 不拒绝已绑定的段**：并发窗口可把别人接诊包里的段静默抢走——不作废原报告、无审计（三条移动路径唯独 bind 裸奔）。修：rec.session_id 非空回 409"请走换绑" |
| C2 | webapp:9872-9887 | **去重路径 DELETE 已绑占位（bind-before-upload 之雷）**：先绑后传的占位撞上去重被直接删——**刚绑好的段凭空蒸发**，完整版躺回待整理。修：DELETE 加 session_id IS NULL；已绑占位应回填而非删除 |
| C3 | webapp:10912 | **D9 不完整（回归）**：direct_rebind 新建 session 漏传 customer_id=NULL → "禁换给自己"客户端过滤+服务端守卫双双失效，报告可被白作废。修：get_or_create_session 传 customer_id；NULL 时姓名兜底比较 |
| C4 | BCVm:409 + webapp:10903 | **D1 回归**：删预补登后换绑到"非本人接待过"的顾客必 400，确认弹窗却还说"会自动补登"——用户被迫重复建档。修：服务端放宽为同公司自动补登（写审计），或候选收窄+文案改口 |
| C5 | webapp:5855,6071 | **免审批直删三重闸缺失**：不验属主（同公司可删同事录音）、不验锁定（分析中照删）、删后非空 session 不作废（done 报告基于已删音频继续展示）。修：补三闸 |
| C6 | webapp:10855,11123 | direct_rebind/unbind 缺"分析中"闸：可把段塞进/拽出 running 的接诊包且不标 outdated。修：running/queued 回 409；候选带 locked |
| C7 | BCVm:150 | boot"不在待整理=已绑定"：已删除的段从提醒深链进来会显示空白换绑界面，确认才撞 404。修：candidates 确认存在性，不存在给明话 |
| C8 | BCVm:200-219 | loadPicks 半渲染：候选接口失败但名单成功时静默合成——D9 排除与 7 天红条双双失效。修：失败整体置 error+重试 |

## G. 报告/空壳 session（P1×3、P2×4）

| # | 位置 | 问题 |
|---|------|------|
| G1 | webapp:6286-6375 | **/api/session/analyze 无 0 录音守卫**：空壳点"重新分析"被写成永久 pending"排队中"→stuck→档案显示"失败"，toast 却说排队中（李雪雪实测路径）。修：对齐 start_analysis 的空包守卫回 400 |
| G2 | webapp:5402,5834,11101 + ArchiveScreen:379 | **空壳 session 无人清理无人过滤**：解绑/换绑/删除移空后壳子留档案、整行可点进报告页。修：列表排除"非当日且 0 录音"，或夜间任务回收 |
| G3 | webapp:6076 + ReportViewModel:83 + ReportScreen:302,449 | **空壳无出口**：报告页无"无片段"空态（仍给重新分析+删除）；删除客户端说审批、服务端 404。修：0 录音给专用空态；服务端 delete-request 遇 0 录音直删回 deleted=true |
| G4 | webapp:5264 ↔ ReportScreen:366 + ArchiveScreen:483 | **状态桶缺 outdated/cancelled（D6/D7 被架空）**：displayStatus 恒非空且两态都落 idle → 报告页"已过期"横幅永不显示、cancelled 空态走不到"已中断"、档案显示"未分析"与接诊页打架。修：服务端桶补两分支（SQL 版同步），或客户端先读 raw status |
| G5 | ArchiveScreen:487 ↔ webapp:5378 | stuck 行 pill 显"失败"但按"失败"筛不出；ArchiveViewModel 7 项筛选是死代码。修：筛选对齐服务端桶 |
| G6 | ReceptionScreen:765 ↔ 档案/报告 | 停滞 session 三处三个说法（待分析/失败/分析失败），"看进度"进预览又变"开始分析"。修：统一"中断，可重试"口径 |
| G7 | ReportScreen:131 | 报告页 15s 轮询无 repeatOnLifecycle（F2 漏网）+任务全 done 后仍无限轮询。修：RESUMED 收口+终态停 |
| G8 | webapp:12386 + SPC:1929↔webapp:10119 | 已绑占位在接诊包"伪装正常段"：preview 不回 upload_status、假 key 照签 url；上传终败后取消被服务端拒（只删未绑）→ **已绑僵尸占位永挂**。修：preview 回 upload_status；服务端对已绑超时占位给自愈 |

## H. 后台保活（P1×2、P2×3）

| # | 位置 | 问题 |
|---|------|------|
| H1 | SPC:1062,609 + PKS:189 | **C2"已被覆盖"只对一半（升级必修）**：录音级保活被拒后 keepAliveOn=true 挡住本会话所有重试，回前台也无人补挂；C9 续租只在 !busy 分支，录音中一次不跑 → vivo 后台开录=整段无 FGS 无唤醒锁。修：挂保活每次真发（PKS 幂等）；回前台按持有态重发；busy 分支也续租 |
| H2 | PMS:65-76 | **STICKY 空 intent 重启不调 startForeground**：录音中被杀→系统重启→FGS 超时击杀/僵尸服务（恢复本来就走补传扫描，STICKY 纯负收益）。修：改 START_NOT_STICKY+null action 防御 |
| H3 | PKS:60,94,119 + SPC:980 | maxStop(3h) 自停不清 static 持有不通知控制器：队列长挂时 3h 后保活悄没但 keepAliveOn=true 永不重挂；反向脏持有又会被后续 OFF 事件拉起"挂了不撤"。修：stopSelfClean 清持有+配合 H1 每次真发 |
| H4 | SPC:1801,1884,1819 | localRaw 离线重试永动机：无网时每 ≤2 分钟重打包重试、唤醒锁连烧 3h → OriginOS 耗电榜挂名反遭用户限制。修：无网不试（交给网络回调）；ogg 打包缓存；退避期降保活档 |
| H5 | PKS:99 + SPC:1090 | STICKY 重启 connHold=true 无校正回路：笔关机时"保持连接中"通知空挂 3h 白耗电。修：短确认窗（10min）没真实事件即自停 |

---

## 修复批次建议

**批次五 · 不丢音频+数据完整性（Z1 + A1-A5 + B1-B4 + C1-C3 + H1-H2，15项）**
真会丢音频/丢绑定/双录两行的全在这批，含 6 处四批修复的回归收口（B3反噬/D12/E8/E6/D9/C2半覆盖）。

**批次六 · 语义一致+出口补齐（C4-C8 + G1-G8 + B5-B9 + H3-H5 + A6-A11，16项）**
空壳 session 三件套、状态口径统一、权限/文案/边角。

> 运维项（改代码前可先做）：①清理存量空壳 session（含李雪雪 100341，先查 analysis_status 是否已被写成
> pending/stuck）；②确认 6-14"未命名"56分钟录音（rec 1547）是否测试残留，是则删。
