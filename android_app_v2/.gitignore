# Local config (machine-specific, never commit)
local.properties
# 注：keystore.properties 与 gongpai-release.jks 应仓库所有者要求**有意提交**到本私有仓库
# （方便迭代查找，含签名口令）。详见 android_app/RELEASE_SIGNING.md。

# Build output
.gradle/
.kotlin/
build/
app/build/
# 迭代期编译日志草稿 / 屏幕截图草稿（根目录锚定，别误伤 res/ 下的图标 png）
/build.log
/build*.log
/v2_*.png

# Android Studio / IntelliJ
.idea/
*.iml
.DS_Store
captures/
