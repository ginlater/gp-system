
android.useAndroidX=true
android.enableJetifier=true
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8 -Dsun.jnu.encoding=UTF-8
# ↑ 强制 UTF-8：本仓库路径含中文(persist创业/sm身美)，否则 Kotlin daemon 会把路径 mangle 成 \u 转义、
#   报假的 "source file not found" 构建失败。固定在此，避免每次手动 export。
# 注：Opus 解码改用 JNA 直接加载 droidkit libopus.so，已不需要 NDK/CMake。
# 原 Demo 在此写死了一台 Mac 的 NDK 路径，换机器会构建失败，故移除。
