# 录音笔音频截断排查清单（Android 端）

> 日期：2026-06-07　范围：顾问端蓝牙录音笔（`consultant-pen`）录音"显示时长 ≫ 实际音频"
> 首例：session 100176（lingyu × 孟非宝宝），rec 626 / 627
> 核心要回答的一句话：**为什么音频在 ~35 秒停了，计时器却还在跑到 9 分钟。**

---

## 现场结论（已在后端坐实）

| id | 录音时间 | 原生/笔上报时长 | ffprobe 实测 | 比值 | 来源 |
|----|----|----|----|----|----|
| 626 | 06-06 22:51:48 | 05分46秒 (346s) | 00分34秒 (34s) | 10.2× | 蓝牙录音笔 |
| 627 | 06-06 23:00:53 | 08分59秒 (539s) | 00分35秒 (35s) | 15.4× | 蓝牙录音笔 |

- 前端"9分钟" = `round(539/60)`，来自原生上报的 `duration_sec`（墙钟）。
- 真实音频只有 34/35 秒，后面 8 分多钟根本没进文件 —— **真实音频丢失，不是后端算错**。
- ASR 时 `_ensure_clean_audio`（`webapp.py:1417`）用 ffprobe 把 `duration_label` 纠正成真实值，所以现在库里已是 00分35秒。但纠正发生在试听之后，且转码成功后会立即删原始 webm（`webapp.py:1446`），**原文件不可恢复**。

## 锁定范围（孤立事件，非通病）

- 全库 33 条已规范化录音里，**只有 626/627 严重缩水**；`consultant-upload`（整文件）全 1.0×。
- 同一支笔的 **rec 616 录了 35 分钟完好（1.0×）** → 笔与编码本身没问题。
- 626/627 是**连续两段**（22:51、23:00），**都**在 ~34–35 秒截断 → 06-06 那次接诊的**特定连接故障**，不是所有笔录音都坏。

---

## 核心假设（最可能）

蓝牙笔在 ~35 秒处掉线，**断线重连逻辑保住了"录音会话"（计时器/UI 状态），但音频写入流没真正恢复** —— 重连后的新流没接续写入原文件，或写文件的 sink 早就停了。结果：文件只有断线前的 35 秒，会话计时器一路跑到用户按停的第 9 分钟。

> 与最近提交直接相关：`断线自动重连 + 去重 + 幽灵录音过滤`、`离线韧性`。bug 很可能就藏在这套重连/过滤逻辑里。
> 两段都 ~35 秒这个"定值"是关键：不像随机丢包，更像断线后某个固定缓冲/sink 停了，或重连有固定超时窗口。

## 具体要查的点（对着原生录音引擎）

1. **断线时音频 sink 怎么处理**：BLE 断开瞬间，写文件的 encoder/muxer 是被关掉/flush 了吗？重连后是**新建文件**还是**续写原文件**？若新建了但 UI 还认旧会话 → 旧文件就停在 35 秒。
2. **计时器的时基**：`__onNativeRecState(state,msg,durSec)` 回推的 `durSec`、最终上传的 `duration_sec`，是按**墙钟（开始→停止）**还是按**实际写入的音频字节/帧**算？现在明显是墙钟 —— **改成按实际写入帧数算**，断线没收到数据时长就不会涨，数字立刻诚实。
3. **"幽灵录音过滤 / 自动重连"逻辑**：重连成功后有没有真的把音频管道重新接到同一个文件 writer？会不会把重连后的数据当幽灵丢了，却没停会话？
4. **断线有没有上报 error 态**：看 `__onNativeRecState` 的 `error` 分支（`web_v2/templates/consultant.html:589`）。断线 35 秒后应进 `error` 或至少提示用户，而不是继续显示"录音中"。这次显然没触发。
5. **后台/锁屏**：22:51–23:09 这段，App 是否被切后台或锁屏导致 BLE/前台服务被系统限制？确认录音是否跑在**前台 Service** 且 BLE 有保活。

## 复现 & 抓证据

- **复现**：连笔开录 → 30 秒后人为断笔（关机/走出范围/关蓝牙）→ 等几分钟 → 按停 → 看上传文件实际多长、`duration_sec` 报多少。大概率直接复现 35s vs 9min。
- **日志**：抓 BLE 连接/断开事件、音频帧接收回调、文件 writer 的写入字节随时间曲线。看断点是否正好卡在 ~35s，以及断点后帧回调是否还在来。

## 验收标准

- 断线后：要么**真续录**（文件时长 ≈ 墙钟），要么**明确报错并提示用户重连/重录**。
- 上传的 `duration_sec` 必须等于**文件里实际音频时长**，不能再是墙钟。这样即使再断，顾问端当场就能看到"只录到 35 秒"，不会等到试听才发现。

---

## 后端可选安全网（老板决定暂不改，待定）

真正根因在 App 原生录音引擎（蓝牙笔流，代码不在本仓库）。后端这边可加两道兜底，把"静默丢失"变"即时提醒"：

1. **检测 + 告警**：转码/上传时比对 上报时长 vs 实测时长，差距大（如上报 ≥60s 且实测 < 上报×50%）就给录音打"疑似丢失"标记，在顾问端未归档列表和报告页醒目提示"上报 X 分钟、实际只录到 Y 秒，可能蓝牙断连，请核对/重录"。
2. **可疑录音保留原文件**：对"实测 ≪ 上报"的录音先别删原始 webm，留作复检/重新提取。
