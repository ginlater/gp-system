# 后端改动说明：录音「占位片段」接口（交给服务器上的 Claude Code 实施）

> 目标：录音笔大文件下载+上传慢（几分钟）。要让顾问**结束录音就立刻在"未归档片段"列表看到一条占位（带服务日期）**，时段/时长/试听等真正传完 OSS 再补上。
> 文件：`webapp.py`（Flask + sqlite）。下面 5 处改动，原生端（安卓 App）已按此契约对接，**接口签名/字段名必须完全一致**。
> 注意：本地 `webapp.py` 已有一份参考实现（可对照），但**请以本服务器的 webapp.py 为准实施**，别直接覆盖。

## 接口契约（原生端依赖，勿改名）
- `POST /api/consultant/placeholder` 表单字段 `recorded_at`(可选, "YYYY-MM-DD HH:MM:SS") → 建一条 processing 占位记录 → 返回 `{"id": <rid>}`。
- `POST /api/consultant/upload` 新增可选表单字段 `placeholder_id` → 若提供且有效，则**回填该占位行**（不新建），返回 `{"id": <placeholder_id>, "oss_key": <真key>}`；不提供则保持原有逻辑。
- `GET /api/consultant/recordings/pending` 每条新增 `upload_status`('processing'|'done')；processing 行 `audio_url=null`。

---

## 1. 迁移：recordings 加列 upload_status
在 `init_db` 里 recordings 的迁移段（和其它 `ALTER TABLE recordings ADD COLUMN` 放一起，`rec_cols` 变量已存在）：
```python
if "upload_status" not in rec_cols:
    conn.execute("ALTER TABLE recordings ADD COLUMN upload_status TEXT DEFAULT 'done'")
```
默认 `'done'`，存量数据不受影响。

## 2. 新接口 `POST /api/consultant/placeholder`
放在 `api_consultant_upload` 函数附近。**用底层 `db_write`（项目自带的写库队列，返回 lastrowid），不要走 `ingest_recording`**（避免建 session/起分析流水线）：
```python
@app.route("/api/consultant/placeholder", methods=["POST"])
@login_required
def api_consultant_placeholder():
    err = _consultant_required()
    if err:
        return err
    u = current_user()
    advisor = u["advisor_name"] or u["username"]
    company_id = u["company_id"] or 1
    now = datetime.now()
    # ★录音真实开始时间(原生传)，用于未归档列表的服务日期/时段；没传则用现在
    recorded_at = request.form.get("recorded_at") or now.strftime("%Y-%m-%d %H:%M:%S")
    ts14 = now.strftime("%Y%m%d%H%M%S")
    # 占位 oss_key：唯一、不真传 OSS，上传完成时会被换成真 key
    placeholder_key = f"pending-uploads/{company_id}/{u['id']}/{ts14}_{_uuid.uuid4().hex[:8]}.pending"
    rid = db_write(
        """INSERT INTO recordings
           (session_id, oss_key, advisor, customer, recorded_at,
            duration_label, size_bytes, source, company_id, uploader_user_id,
            upload_status, created_at)
           VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)""",
        (None, placeholder_key, advisor, None, recorded_at,
         None, 0, "consultant-pen-placeholder", company_id, u["id"],
         "processing", now.strftime("%Y-%m-%d %H:%M:%S")),
    )
    return jsonify({"id": rid})
```

## 3. 改 `POST /api/consultant/upload` 支持回填占位
在 `data = f.read()` 之后、原 `oss_bucket.put_object` 之前插入回填分支（其余原逻辑保持）：
```python
    data = f.read()
    # 回填占位：结束录音时已先建 processing 占位记录，这里把真音频补上，不新建行
    placeholder_id = request.form.get("placeholder_id")
    if placeholder_id:
        prow = db_fetchone(
            "SELECT id, uploader_user_id, upload_status FROM recordings WHERE id=?",
            (placeholder_id,),
        )
        if prow and prow["uploader_user_id"] == u["id"] and prow["upload_status"] == "processing":
            try:
                oss_bucket.put_object(oss_key, data)
            except Exception as e:
                app.logger.exception("顾问端上传 OSS 失败")
                return jsonify({"error": _friendly_oss_error(e)}), 500
            db_write(
                """UPDATE recordings SET oss_key=?, size_bytes=?, duration_label=?,
                   source='consultant-pen', upload_status='done' WHERE id=?""",
                (oss_key, len(data), dur_label, prow["id"]),
            )
            return jsonify({"id": prow["id"], "oss_key": oss_key})
    # ↓ 原有"新建"逻辑保持不变（put_object + ingest_recording + return）
```
（`oss_key`/`dur_label`/`recorded_at` 都用原函数里已算好的变量。占位回填保留占位行的 `recorded_at`，所以时段以录音真实开始时间为准。）

## 4. 改 `GET /api/consultant/recordings/pending` 输出 upload_status
- 两个 SELECT 的列里都加上 `upload_status`。
- 组装每条 `d` 时：
```python
    d["upload_status"] = r["upload_status"] if "upload_status" in r.keys() else "done"
    if d["upload_status"] == "processing":
        d["audio_url"] = None        # 占位 key 不是真 OSS，别签名（会坏）
    else:
        try:
            d["audio_url"] = oss_signed_url(d["oss_key"], expires=3600)
        except Exception:
            d["audio_url"] = None
```
其余 service_date/start_hm/end_hm/duration_min 逻辑不变（占位 recorded_at 有值→日期/起始时段能算；duration_label 为空→duration_min=0；网页会把 processing 行渲染成"处理中"）。

## 验证
- `python3 -c "import ast; ast.parse(open('webapp.py').read())"` 语法通过。
- 重启服务后：原生 App 结束录音 → `pending` 里立刻出现一条 `upload_status=processing`、有 `service_date`、`audio_url=null` 的记录；几分钟后音频传完 → 同一条变 `upload_status=done`、有 `audio_url`/`duration`。
- 兼容性：不传 `placeholder_id` 的旧上传、存量记录（upload_status 默认 done）行为不变。

---

## 5b. 新接口 `POST /api/consultant/placeholder/cancel`（清理失败占位，必须加）
原生在某段录音**最终上传失败被丢弃**时会调它，删掉那条"处理中"占位，避免脏数据永久残留在未归档。放在占位接口附近：
```python
@app.route("/api/consultant/placeholder/cancel", methods=["POST"])
@login_required
def api_consultant_placeholder_cancel():
    err = _consultant_required()
    if err:
        return err
    u = current_user()
    pid = request.form.get("placeholder_id")
    if not pid:
        return jsonify({"error": "缺少 placeholder_id"}), 400
    row = db_fetchone(
        "SELECT id, uploader_user_id, upload_status, session_id FROM recordings WHERE id=?",
        (pid,),
    )
    # 只删本人、仍 processing、未绑定 session 的占位（安全）
    if row and row["uploader_user_id"] == u["id"] and row["upload_status"] == "processing" \
            and row["session_id"] is None:
        db_write("DELETE FROM recordings WHERE id=?", (row["id"],))
        return jsonify({"ok": True})
    return jsonify({"ok": False})
```
> ⚠️ 复核：第 2 节占位接口里 `recorded_at = request.form.get("recorded_at") or now.strftime(...)` —— **务必读原生传的 recorded_at**（录音真实开始时间），否则未归档的服务日期/时段会用"到达时刻"而非录音起点，跨零点会错日期。
