# 前端真机测试清单 · 录音笔 → .ogg 上传 / 占位 / recorded_at

> 给安卓 App（前端 CC）在**真机**上跑的验收清单。后端已上线（2026-06-05），对应改动：
> 手机改传**标准 `.ogg`**、新增 `recorded_at`、占位接口全套已上线。
> 用法：按场景做操作，对照"应看到"，对不上就看"对不上时"那栏，需要查库的找后端（我）报 **recId / 顾问名 / 大致时间**即可。

---

## 0. 后端已就绪的契约（前端按此对接，字段名勿改）

| 接口 | 方法 | 关键字段 | 返回 |
|---|---|---|---|
| `/api/consultant/placeholder` | POST | `recorded_at`(可选, `YYYY-MM-DD HH:MM:SS`) | `{"id": 占位rid}` |
| `/api/consultant/upload` | POST | `file`(=xxx.ogg), `duration_sec`, `placeholder_id`(回填该行), `recorded_at`(真实开始时间), `sn`(可空) | `{"id":rid, "oss_key":..}` |
| `/api/consultant/placeholder/cancel` | POST | `placeholder_id` | `{"ok":true/false}` |
| `/api/consultant/recordings/pending` | GET | — | 每条带 `upload_status`(`processing`/`done`)；`processing` 时 `audio_url=null` |

- 未归档列表（"未归档片段"）**每 5 秒自动刷新**；处理中的占位行显示 **"处理中，请稍候"**，只给服务日期，时段/时长/试听等传完再补。
- ✅ 后端已自测：`.ogg` 能被 ffmpeg/DashScope 正常解码转写；线上 healthz 200、三接口已注册、`upload_status` 列已迁移。

---

## 场景 1 · 单段录音 happy path（最基本，先过这个）

**做什么**
1. 用录音笔录一段（建议 1–2 分钟），正常结束。

**应看到**
- 录音一结束，**几秒内**未归档列表就冒出一条 **"处理中，请稍候"**（占位），带**今天的服务日期**。
- 等录音笔大文件下载完 + 上传完（可能几十秒到几分钟）→ **同一条**变成正常行：有时段、有时长、能**试听**。
- 点试听 → 浏览器能正常播放（`.ogg` 后端会转成 wav 再给播放）。

**对不上时**
- 占位一直不出现 → 检查 App 有没有先调 `/placeholder`，或 `_consultant_required` 是否过（必须是顾问账号登录）。
- 一直停在"处理中"不变 done → 上传没成功；看 App 是否调了 `/upload` 且带了 `placeholder_id`；让后端查该 rid 的 `upload_status`。
- 试听点不响 → 让后端查该 rid 的 `oss_key` 是否 `.ogg`、ASR 后是否已转成 `_clean.wav`。

**让后端查**：报 recId → 我确认 `oss_key` 以 `.ogg` 结尾、`upload_status=done`、`recorded_at`=真实录音时间。

---

## 场景 2 · recorded_at 真实录音时间（本轮重点，务必测）

录音笔大文件"下载+上传"很慢，**录音开始时刻**和**传完入库时刻**天然差几分钟——这正是要验证 `recorded_at` 没被传错。

**做什么 & 应看到**
1. **普通段**：录一段、等传完。未归档/接诊里显示的**服务日期 + 时段**应是**录音开始**那一刻，**不是**上传完成那一刻。
2. **跨零点（关键）**：在 **23:50 左右**开始录一小段，让它**零点后**才传完。
   - 应看到：服务日期 = **开始录音那一天**（昨天），**不是**传完那天（今天）。
   - 这条最容易暴露 bug，一定要测。
3. **连录多段**：连着录 2–3 段。每段的时间应是**各自的开始时间**，不会几段全挤在同一个"上传时刻"。

**对不上时**
- 时间变成"上传时刻" / 跨零点错成今天 → App `/placeholder` 或 `/upload` 没带 `recorded_at`，或格式不是 `YYYY-MM-DD HH:MM:SS`。后端做了多格式兜底（也认 `T` 分隔、到分钟），但**最好按标准格式发**。

**让后端查**：报 recId + 你记的真实开始时间 → 我对 `recordings.recorded_at` 是否一致。

> 机制说明（给前端理解用）：占位行建的时候就用 App 传的 `recorded_at`；上传回填时，**只有当本次 upload 也带了 `recorded_at` 才覆盖**，没带就保留占位时的真实时间——所以两端都发 `recorded_at` 最稳。

---

## 场景 3 · 占位回填是"同一行"，不新建

**做什么**：录一段，盯住未归档列表那条占位。

**应看到**：上传完成后是**那一条**从"处理中"变 done，列表**不多出一条**（不会变成"占位一条 + 真录音一条"）。

**对不上时**：若出现两条（一条处理中残留 + 一条新的）→ App `/upload` 没带 `placeholder_id`，或带的 id 不是本人的占位 / 已不是 `processing` 状态。

**让后端查**：报顾问名 → 我看是否有重复行、占位 id 前后一致。

---

## 场景 4 · 上传失败 → 占位被清掉（不留脏数据）

**做什么**：录一段后，制造上传失败（如**录完立刻断网/关流量**让 `/upload` 失败），App 放弃这段时应调 `/placeholder/cancel`。

**应看到**：那条"处理中"占位**消失**，未归档列表里不残留僵尸占位。

**对不上时**：占位永久卡在"处理中" → App 上传失败分支没调 `/placeholder/cancel`，或带的 `placeholder_id` 不对。

> 注：cancel 只会删**本人、仍 processing、未绑定**的占位（后端安全限制），所以"已传完/已绑定"的不会被误删。

---

## 场景 5 · .ogg 能转写（ASR）

**做什么**
1. 把一段 `.ogg` 录音**绑定到顾客**（未归档 → 选/建顾客绑定）。
2. 按产品规则**手动触发分析**（"待分析 / ⟳ 重跑"，分析是手动的不自动）。

**应看到**：ASR 出**转写文字**（不是空、不报错）；随后能出分析报告。

> 可直接拿已经在库里的真机录音 **recId 590（.ogg）/ 588（webm）** 来测：现在是 `awaiting_intake`（未绑定），绑定 + 触发后应能转写。

**对不上时**：转写空/报错 → 让后端看该 rec 的 `asr_status` / `asr_error`（后端已验证 ffmpeg 能解 ogg，多半是绑定/触发环节）。

---

## 场景 6 · 全链路 + 回归不破

1. **全链路**：录音 → 占位 → 上传 → 未归档 → 绑定顾客 → 手动分析 → 报告，`.ogg` 整条走通。
2. **回归（别破老功能）**：
   - 网页端老的录音（webm）/老的手动上传**仍正常**入库、转写、播放。
   - 老的未归档记录（`upload_status` 默认 `done`）显示正常，能试听。
   - 已绑定/已分析的历史录音不受影响。

---

## 7. 一次性自检小抄（前端跑完勾一遍）

- [ ] 录完几秒内出"处理中"占位，带今天日期
- [ ] 传完同一行变 done，可试听，浏览器能播 .ogg
- [ ] 服务日期/时段 = 录音**开始**时间（非上传时间）
- [ ] **跨零点**那段日期 = 开始那天（昨天），没错成今天
- [ ] 连录多段，各段时间互不挤压
- [ ] 占位回填是同一行，不新增重复行
- [ ] 上传失败那段，占位被 cancel 清掉、不残留
- [ ] 绑定 + 手动分析后，.ogg 出转写文字
- [ ] 老的 webm / 手动上传 / 历史记录回归正常

> 任一项不过，把 **recId / 顾问名 / 你记的真实录音时间 / 现象** 发我，我从后端库和日志侧帮你定位。
