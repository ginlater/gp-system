# 录音笔稳定性 · 统一真机验收清单（iOS 对照版）

> **用途**：这是 v2.0.61「录音笔稳定性修复发版」全部改动的**唯一总验收清单**，由四份零散清单合并而成。
> Android 端已真机验证过；本文档供 **iOS 端重新逐条复测**，确认同样的能力在 iOS 上也成立。
> **读法**：每个模块 = ①Android 改了什么 → ②iOS 对应实现点 → ③真机怎么操作 → ④期望结果 → ⑤判定勾选。
> 任一项不过，记下 **recId / 顾问名 / 真实录音时间 / 现象 + 截图** 反馈，后端可从库和日志侧定位。
>
> **平台差异前提（务必先读）**：
> - iOS 蓝牙笔走**声云 PNote SDK**（`libPNote.a` = Obj-C + CoreBluetooth，见 `蓝牙_声云录音卡片sdk518.zip`），**不是**杰理。
>   下面 Android 提到的 `PenController` / `ATWOpusConverter` / `penlog.txt` 等是杰理链路的命名，iOS 需在 PNote 链路上找到**功能等价点**复测，行为目标一致即可。
> - iOS 发布走 **App Store，无强制更新**；后端契约（占位/upload/recorded_at/断网 toast）两端共用、**后端无需再改**。
> - 蓝牙物理断连无法 100% 杜绝；目标是「**尽量不断 + 断了也不丢、不入库截断件、时长诚实**」。

---

## 背景：要解决的根因（一句话）

录音笔实时流中途**静默卡死/断连**，计时器/UI 却继续跑到结束 → 文件只有断点前几十秒，却按**墙钟**上报成 9 分钟（lingyu「显示9分钟、实际35秒」，rec 626/627，比值 10~15×）。
四层加固：**降断连**（保活）+ **断了不丢**（看门狗回落补下载）+ **不入库截断件**（防截断闸 + 时长诚实）+ **后端兜底告警**。

---

# 一、防断连 · 录音中保活（核心）

**Android 改了什么**：新增前台保活 Service `PenKeepAliveService`（`connectedDevice` 类型 + `PARTIAL_WAKE_LOCK`），录音笔录音**整段**托住进程，防被切后台/锁屏/息屏挂起导致 BLE 回调停。录音中常驻通知，按停后通知消失、唤醒锁释放（8 个出口已配平无泄漏）。

**iOS 对应点**：开启 **Background Modes → Audio + Uses Bluetooth LE accessories**；录音整段保持 `AVAudioSession` 活跃 + CoreBluetooth 后台连接，避免 App 挂起后 PNote 回调中断。iOS 无前台常驻通知概念，可用「正在录音」状态指示替代。

| # | 真机操作 | 期望 | 勾选 |
|---|---|---|---|
| 1 | **锁屏续录**：连笔开录 → 立刻锁屏息屏 → 等 **5 分钟** → 解锁按停 | 录音时长 ≈ 5 分钟，**音频实测 ≈ 墙钟**，不缩水 | [ ] |
| 2 | **切后台续录**：开录 → 切微信/桌面停留 **5 分钟** → 回来按停 | 同上不缩水 | [ ] |
| 3 | **长待机**：开录 → 锁屏放置 ≥10 分钟 | iOS 未被系统回收，回调持续，时长不缩水 | [ ] |
| 4 | **录音状态可见**：录音中有明确「录音中」指示；按停后指示消失 | 状态正确，无残留 | [ ] |

> Android 验收重点是国产 ROM 杀后台；iOS 等价风险是 App 被挂起/回收，所以 1~3 是 iOS 的核心项。

---

# 二、断了不丢 · 卡流看门狗

**Android 改了什么**：`streamStallWatch` —— 实时流来过、但 **>6s 没新帧**（连着但没数据）→ 标本段流不完整 → 收尾**回落「从笔机身补下载」**拿全段（笔机身一直在录，音频没丢）。

**iOS 对应点**：在 PNote 实时流回调上做同样的「最近一帧时间戳」监控，>6s 无新帧即标记本段不完整，收尾走机身补下载。

| # | 真机操作 | 期望 | 勾选 |
|---|---|---|---|
| 1 | **人为断流**：开录 30s → 关笔 / 走出范围 / 关蓝牙 制造断连 → 等 1 分钟 → 按停 | 要么自动重连续录、要么明确提示「将从录音笔补取」；**最终库里音频实测时长 ≈ 真实录到的**，不再「9分钟/35秒」 | [ ] |
| 2 | **补下载兜底**：断流那条最终通过机身补下载拿到全段 | OSS 文件 ffprobe 时长 ≈ 墙钟（机身一直在录） | [ ] |
| 3 | **日志佐证**（iOS 看等价日志/诊断）：能看到「实时流卡死无新帧」「回落补下载」记录 | 有对应日志事件 | [ ] |

---

# 三、不入库截断件 · 防截断直传闸（最后一道闸）

**Android 改了什么**：`finishSessionEnqueue` 收尾时，实时流估算秒数（≈bytes/4000）**< 墙钟时长 × 0.6 且墙钟 ≥30s** → 判疑似截断 → **绝不直传截断件**，回落补下载。

**iOS 对应点**：收尾前用同样比值判据拦截，疑似截断走机身补下载而非直传。

| # | 真机操作 | 期望 | 勾选 |
|---|---|---|---|
| 1 | 制造一段「墙钟长、实际流短」的录音（同模块二人为断流） | 不直传截断件，自动回落补下载 | [ ] |
| 2 | **诚实时长**：上报 `duration_sec` = **文件实际音频时长**，非墙钟 | 即使再断，顾问端当场看到「只录到 X 秒」，不必等试听才发现 | [ ] |

> 验收标准（来自截断排查清单）：断线后**要么真续录（文件≈墙钟），要么明确报错提示重连/重录**；`duration_sec` 必须 = 文件实际音频时长。

---

# 四、后端时长不符告警（两端共用，iOS 只验端上展示）

**后端改了什么**（已上线，无需再改）：
- 上传即测 `_detect_truncate_note`：上报墙钟 ≥60s 且 ffprobe 实测 < 上报 ×50% → 打 `truncate_note`，未归档列表当场红/黄条提醒。
- ASR 处 `_ensure_clean_audio` 同样比对，命中则打标 + **保留原始件不删**（破例留证）。

| # | 真机操作 | 期望 | 勾选 |
|---|---|---|---|
| 1 | 若仍有截断件混入 | 未归档列表该条出现黄条「上报X分、实际只录到Y秒，疑似蓝牙断流丢失」 | [ ] |
| 2 | 该条绑定+分析后 | 原始截断件**未被删**（OSS 还在），日志有 `[truncate] rec N …保留原始件` | [ ] |

---

# 五、.ogg 上传 / 占位 / recorded_at（链路对齐）

**契约（后端已上线 2026-06-05，字段名勿改）**：

| 接口 | 方法 | 关键字段 |
|---|---|---|
| `/api/consultant/placeholder` | POST | `recorded_at`(可选) → `{"id":占位rid}` |
| `/api/consultant/upload` | POST | `file`(xxx.ogg), `duration_sec`, `placeholder_id`, `recorded_at`, `sn`(可空) |
| `/api/consultant/placeholder/cancel` | POST | `placeholder_id` |
| `/api/consultant/recordings/pending` | GET | 每条带 `upload_status`(`processing`/`done`) |

**iOS 对应点**：PNote 裸 opus 帧端上包成**标准 OGG Opus（.ogg）**再传（Android 已用 `ATWOpusConverter` 重打包，iOS 需在 PNote 链路产出等价 .ogg；opus 链路同 Android 零改动）。录完先调 `/placeholder`，传完用 `placeholder_id` 回填同一行。

| # | 场景 | 期望 | 勾选 |
|---|---|---|---|
| 1 | **happy path**：录 1–2 分钟正常结束 | 几秒内未归档冒「处理中，请稍候」占位（今天日期）→ 传完**同一条**变 done、有时段/时长、能试听，浏览器能播 .ogg | [ ] |
| 2 | **recorded_at 普通段** | 显示的服务日期+时段 = **录音开始**时刻，非上传完成时刻 | [ ] |
| 3 | **跨零点（关键）**：23:50 左右开录、零点后才传完 | 服务日期 = **开始那天（昨天）**，没错成今天 | [ ] |
| 4 | **连录多段**：连录 2–3 段 | 每段时间 = 各自开始时间，不全挤在「上传时刻」 | [ ] |
| 5 | **占位回填同一行**：盯住占位那条 | 上传完是**那一条**从处理中变 done，**不多出一条**（不会占位+真录音各一条） | [ ] |
| 6 | **上传失败清占位**：录完立刻断网/关流量让 upload 失败 | App 放弃时调 `/placeholder/cancel`，那条「处理中」**消失**，无僵尸占位 | [ ] |
| 7 | **.ogg 转写**：绑定顾客 → 手动触发分析（分析是手动的） | ASR 出转写文字（非空非报错）→ 能出分析报告 | [ ] |

> 占位回填机制：上传回填时**只有本次 upload 也带 recorded_at 才覆盖**，所以两端都发 recorded_at 最稳。

---

# 六、网页端断网错误提示（consultant.html，两端共用 WebView）

**改了什么**（commit `41d3e84`，online-0527，后端无改动）：包了一层 `window.fetch`，**所有联网操作**（28 处 fetch）断网失败弹统一 toast **「⚠ 网络断开，请检查网络后重试」**；单条常驻不刷屏，恢复后最后一次失败 3.5s 自动消失；服务器 4xx/5xx **不触发**（那不是断网）。

**iOS 对应点**：consultant 页若用 WKWebView 承载则行为一致；制造断网用**飞行模式**或关 WiFi+蜂窝。

| # | 断网下操作 | 期望 | 勾选 |
|---|---|---|---|
| 1 | 未归档列表自动刷新（每5s） | 弹「网络断开」，**不刷屏**（只一条常驻） | [ ] |
| 2 | 点试听 | 弹「网络断开，无法试听」（仅确实离线时） | [ ] |
| 3 | 绑定/换绑/退回/确认说话人/新增接诊/搜索顾客 | 都弹「网络断开」，**不误以为成功** | [ ] |
| 4 | 触发/重跑/开始分析 | 弹「网络断开」，不假装已开始 | [ ] |
| 5 | 点提醒「已读/已处理」 | 弹「网络断开」 | [ ] |
| 6 | **恢复联网** | toast 自动消失、列表恢复刷新、之前失败操作重试都成功 | [ ] |
| 7 | **不误报（负向）**：联网正常下触发服务器会拒绝的操作（漏填必填项等） | 走该操作**原有报错**，**不弹**「网络断开」；全程正常浏览也不弹 | [ ] |
| 8 | **配套**：断网时录音界面不白屏、按笔录音照常、恢复后自动补传不丢 | iOS 端能力同样成立 | [ ] |

---

# 七、回归（别改坏正常流程）

| # | 项 | 期望 | 勾选 |
|---|---|---|---|
| 1 | 正常一段完整录音（全程不断） | 时长正确、能试听全程、能绑定、能分析，**不误报截断** | [ ] |
| 2 | 短录音（<30s / <60s） | 不触发截断判定、不触发上传 ffprobe（<60s 直接跳过探测） | [ ] |
| 3 | 手机麦克风录音（iOS 本机麦，m4a/对应格式） | 完全不受蓝牙笔改动影响，照常录、传、绑、析 | [ ] |
| 4 | 连录多段、断线补传、幽灵录音过滤 | 行为同改前 | [ ] |
| 5 | 老的 webm / 手动上传 / 历史已绑定已分析记录 | 回归正常入库、转写、播放 | [ ] |

---

## 验收小抄（iOS 跑完整体勾一遍）

- [ ] 锁屏/切后台 5 分钟续录不缩水（保活/后台音频生效）
- [ ] 人为断流 → 不出现「9分钟/35秒」，回落机身补下载拿全段
- [ ] 上报 `duration_sec` = 实际音频时长（非墙钟），断了当场可见
- [ ] 截断件不直传；混入的截断件后端打黄条 + 原始件不删
- [ ] 录完几秒内出「处理中」占位 → 同一行变 done → .ogg 浏览器能播
- [ ] recorded_at = 录音开始时间；**跨零点不错日期**；连录多段不挤压
- [ ] 上传失败 → 占位被 cancel 清掉，无僵尸
- [ ] 绑定+手动分析 → .ogg 出转写文字
- [ ] 断网时所有联网操作弹统一 toast、不刷屏、恢复后自动消失、不误报
- [ ] 手机麦 / 老格式 / 历史记录全回归正常

> 反馈格式：**recId / 顾问名 / 真实录音开始时间 / 现象 / 截图**；怀疑接口问题附**接口名 + 大致时间**。
