# 合作开发手册 · 工牌接诊 App + 后端

> 跨环境协作的单一信息源。商用项目（医美「工牌接诊 / 美丽陪伴」录音分析系统）。
> 两层：① 线上 Flask 后端（本仓库 `webapp.py` + `web_v2/templates/`）② Android 壳 App（`android_app/`，WebView + 原生录音引擎）。
> App 加载线上接诊页：`START_URL = https://gp.aibeautyfulwomen.com/consultant`。

最后更新：2026-06-02。

---

## 1. 线上服务器（新版后端 = 这台）

| 项 | 值 |
|---|---|
| 角色 | **新版 App 后端**。旧的 `43.136.130.133` 是老服务，已不再走 |
| 主机 / 公网 IP | `VM-4-13-ubuntu` / **`110.40.170.227`** |
| 域名 | `gp.aibeautyfulwomen.com` A 记录 → 110.40.170.227 |
| 代码目录 | `/opt/gp-system`（git，分支 `online-0527`，远端 github.com/ginlater/gp-system） |
| Python 环境 | venv：`/opt/gp-system/venv`（flask/oss2/dashscope/anthropic/openpyxl/gunicorn） |
| pip 装包 | 走官方源 + 代理：`pip install --proxy http://127.0.0.1:7890 --index-url https://pypi.org/simple <pkg>`（腾讯内网镜像缺包，必须这样装） |
| 后端服务 | systemd **`gongpai`** → `gunicorn --workers 1 --threads 8 --bind 127.0.0.1:5058 webapp:app`，**非 debug** |
| 环境变量 | `EnvironmentFile=/opt/gp-system/.env`（webapp 直接读 os.environ，不自动加载 .env） |
| 反代 | 系统 **nginx**：`80→443` 跳转；`443` TLS → `127.0.0.1:5058`；`client_max_body_size 500m`（保证录音上传不被挡） |
| 证书 | `/etc/ssl/gp/gp.crt` + `gp.key`（key 600 root）。TrustAsia DV，有效期至 **2026-08-21** |

`.env` / `venv/` / `gpssl.zip` / `.env.bak*` 均已 **gitignore，勿提交**（含密钥）。

---

## 2. 开发 / 部署流程（⚠️ 关键）

**改网页或后端**（`webapp.py`、`web_v2/templates/*`）：
```
# 本地改 → 提交推送
git add ... && git commit && git push origin online-0527
# 在这台服务器上生效
cd /opt/gp-system && git pull && sudo systemctl restart gongpai
```
> 非 debug 模式 Jinja 模板有缓存，**改模板必须 restart gongpai 才生效**。

**改原生 App**（`android_app/`）：
```
# 本地改 → 推送
git push origin online-0527
# 在 Mac 上
git pull → Android Studio 重新构建 debug → 覆盖安装到手机
```
> 网页 JS 对桥做了 `typeof` 容错：单边先上不会崩，但**两边都到位**功能才完整。

---

## 3. 常用运维命令

```
# 后端
sudo systemctl restart gongpai
journalctl -u gongpai -f                         # 看日志
curl -s -o /dev/null -w '%{http_code}\n' http://127.0.0.1:5058/login   # 本地探活(期望200)
# 反代
sudo nginx -t && sudo systemctl reload nginx
curl -I https://gp.aibeautyfulwomen.com/login    # 公网探活(期望200)
```

---

## 4. App 录音设计（现状）

- **录音 UI 全在登录后的「美丽陪伴」页**：原有录音按钮在 App 内通过 `window.AndroidBridge` 驱动原生录音引擎；**右上角「🖊录音笔 / 🎤手机」切换，默认录音笔**；选录音笔但未连蓝牙→点录音 `confirm` 强制提示去连。**已无原生底部录音条**（旧版那条会在每页显示，已删）。
- **原生引擎**：
  - 手机麦 `recording/PhoneMicService`（前台 Service，m4a，切后台/锁屏不中断）；
  - 录音笔 `PenController`（AIREC BLE SDK，**B 模式**：设备本地录音→蓝牙下载文件→必要时 Opus 转 wav→上传）。
  - 两者共用 `RecordingBus` → 上传 `/api/consultant/upload`（带 WebView 登录 cookie）→ 网页 `__onNativeUploaded(id)` 复用既有「刷新未归档 + 绑定客人」流程。
- **桥方法**（`WebAppBridge`）：`isApp / startRecording(source) / stopRecording / getState / getSources / isPenConnected / connectPen / openPenManager`。
- **网页回调**（`consultant.html`，IN_APP 内）：`__onNativeRecState / __onPenConnChanged / __onNativeUploaded`。

---

## 5. ⚠️ 待真机 + 配对录音笔验证（`PenController` 里有 TODO）

1. **START_URL** 已确认走 gp.aibeautyfulwomen.com（DNS 已指这台），无需改。
2. 录音笔停录后 `onRecordStateChanged(false, fileName)` 的 fileName 是否就是文件列表里的名字、停录后多久 `fetchFileList` 能拿到。
3. 笔下载文件的真实格式/扩展名；`AudioConverter.toWav` 能否转；后端是否收（后端接受 webm/mp3/wav/m4a/mp4/ogg/aac/amr，**不收 opus**）。
4. `AIRECBleFile.getDurationSec()` 是否数字（`getCreateTime()` 已确认是 String）。
5. SDK 回调在 `ConsultantActivity`/`PenController` 与 demo `MainActivity` 之间的归属互斥。
6. `ScanActivity` 自行申请 BLE 权限是否够。

---

## 6. 证书续期（到期前必做）

证书 **2026-08-21 到期**（手动证书，TrustAsia DV，**不自动续期**）。续期步骤：
1. 腾讯云 SSL 控制台重新下载 `gp.aibeautyfulwomen.com` 的 Nginx 格式证书；
2. 替换 `/etc/ssl/gp/gp.crt`（bundle.crt）和 `/etc/ssl/gp/gp.key`；
3. `sudo nginx -t && sudo systemctl reload nginx`。

已通过 `/schedule` 安排 **2026-08-07 提醒**。

---

## 7. 其它注意

- **1Panel**：本机装了 1Panel（端口 8090），但公网 Web 用的是**系统 nginx**（因为直接用了你给的证书），它占着 80/443。**以后别在 1Panel 里再建占用 80/443 的站点**，会冲突。
- 老板账号（来自旧 HANDOFF，登录用）：`diajie` / `Diajie2026!`。
- OSS：bucket=gongpai-asr，region=oss-cn-chengdu；ASR：DashScope fun-asr；Claude 走代理 `127.0.0.1:7890`。

## 8. 关键文件索引
- `android_app/HANDOFF_NEXT.md` — 原生侧详细交接（构建、SDK、架构）
- `android_app/README.md` — App 构建/安装说明
- 项目记忆 `recording-pen-and-app-plan` — 决策与背景
