# 录音笔稳定性改动 · 真机测试清单

> 日期：2026-06-07　范围：顾问端蓝牙录音笔（`consultant-pen`）「录音中断连 / 录到一半丢」两层加固
> 背景：lingyu 多条录音「显示9分钟、实际35秒」，根因＝蓝牙实时流中途静默卡死/断连，截断件却当完整上传。
> 本次改动只在掌控内的层面加固；**蓝牙物理断连无法 100% 杜绝**，目标是「尽量不断 + 断了也不丢、不入库截断件」。

---

## 改了什么（4 处）

### 第1层 · 降断连
1. **新增前台保活 Service** `recording/PenKeepAliveService.java`（`connectedDevice` 类型 + `PARTIAL_WAKE_LOCK`）。
   录音笔录音**整段**托住 App 进程，防被切后台/锁屏/息屏挂起 → BLE 回调停。
   - manifest 加 `FOREGROUND_SERVICE_CONNECTED_DEVICE` / `WAKE_LOCK` 权限 + service 声明。
   - `PenController` 在 `doStartRecord` / 镜像 `sessionActive=true`（笔自发开录）/ 连上即在录 三处起保活；
     在 收尾入队 / 没真录 / 重连宽限到 / 笔没确认 / 启动异常 / 非录音断开 各路径停保活（共 8 个出口，已核对配平，无唤醒锁泄漏）。
   - 兜底：Service 自带 3h 最大保活 + 唤醒锁超时，防某条出口漏停。

### 第2层 · 断了也不丢、不入库截断件
2. **录音中卡流看门狗**（`PenController.streamStallWatch`）：实时流来过、但 >6s 没新帧（连着但没数据）→ 标本段流不完整 → 收尾回落「从笔机身补下载」拿全段。
3. **防截断件直传（最后一道闸）**（`finishSessionEnqueue`）：实时流估算秒数（≈bytes/4000）< 墙钟时长×0.6 且墙钟≥30s → 判疑似截断 → 同样回落补下载，**绝不直传截断件**。

### 第3层 · 后端兜底告警
4. **后端时长不符告警**（`webapp.py`）：
   - 上传即测（`_detect_truncate_note`）：上报墙钟≥60s 且 ffprobe 实测<上报×50% → 打 `truncate_note`，**未归档列表当场红/黄条提醒**。
   - ASR 转码处（`_ensure_clean_audio`）同样比对，命中则打标 + **保留原始件不删**（破例留证供复检）。
   - `recordings` 自动迁移加 `truncate_note` 列；未归档 API + 接诊页前端横幅已接。

---

## 必测项（真机 + 真笔，我这边无法驱动蓝牙）

> 装包：`android_app/app/build/outputs/apk/release/app-release.apk`（已编译通过、manifest 合并含新 Service/权限）。

### A. 保活降断连（核心）
- [ ] **锁屏续录**：连笔开录 → 立刻锁屏息屏 → 等 5 分钟 → 解锁按停。期望：录音时长 ≈ 5 分钟（音频实测≈墙钟），通知栏全程有「录音笔录音进行中」。
- [ ] **切后台续录**：开录 → 切到微信/桌面停留 5 分钟 → 回来按停。期望：同上不缩水。
- [ ] **国产 ROM 重点**（vivo/oppo/小米/荣耀）：先确认弹过「忽略电池优化」并已允许；重复 A 两项。这类机型最容易杀后台，是验收重点。
- [ ] **通知存在**：录音中下拉通知栏能看到保活常驻通知；按停后通知消失（保活已停、唤醒锁已放）。

### B. 断了不丢 / 不入库截断件
- [ ] **人为断流**：开录 30s → 关笔/走出范围/关蓝牙制造断连 → 等 1 分钟 → 按停。
      期望：要么自动重连续录、要么明确提示「本段只保存到断开前/将从录音笔补取」；**最终库里这条的音频实测时长 ≈ 真实录到的，不再出现「9分钟/35秒」**。
- [ ] **补下载兜底**：断流那条最终应通过「从笔机身补下载」拿到全段（笔机身一直在录）。核对 OSS 文件 ffprobe 时长 ≈ 墙钟。
- [ ] **看 penlog**：`Android/data/<包名>/files/stream_ops/penlog.txt` 应能看到 `保活↑ / 保活↓`、必要时 `★实时流卡死无新帧` 或 `★疑似截断…回落补下载`。

### C. 后端告警
- [ ] 若仍有截断件混进来：未归档列表该条出现黄条「上报X分、实际只录到Y秒，疑似蓝牙断流丢失」。
- [ ] 该条绑定+分析后，原始截断件**未被删**（OSS 上还在），日志有 `[truncate] rec N …保留原始件`。

### D. 回归（别改坏正常流程）
- [ ] 正常一段完整录音（全程不断）：时长正确、能试听全程、能绑定、能分析。**不应误报截断**。
- [ ] 短录音（<30s / <60s）：不触发截断判定、不触发上传 ffprobe（探测对 <60s 直接跳过）。
- [ ] 手机麦克风录音（`PhoneMicService`，m4a）：完全不受本次改动影响，照常。
- [ ] 连录多段、断线补传、幽灵录音过滤：行为同改前。

---

## 部署注意
- **后端**：`webapp.py` 改了 → 部署后 `sudo systemctl restart gongpai`（迁移 `truncate_note` 列在启动时自动跑）。见 memory `prod-deploy-topology`。
- **APK**：需重新出包发安装。`/download` 页直链会取最新 `app-release.apk`。
